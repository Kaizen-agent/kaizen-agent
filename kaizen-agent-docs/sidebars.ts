import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

/**
 * Sidebar configuration for Kaizen Agent documentation
 */
const sidebars: SidebarsConfig = {
  docs: [
    'intro',
    'quickstart',
    'quickstart_llamaindex',
    'usage',
    'github',
    'examples',
    'faq',
    'limitations',
  ],
};

export default sidebars;
